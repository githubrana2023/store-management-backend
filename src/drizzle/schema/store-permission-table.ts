import { pgTable, uuid, timestamp, uniqueIndex, index, text } from "drizzle-orm/pg-core";
import { storeRolesTable } from "./store-role-table.js";


/* ========================================================= STORE PERMISSIONS ========================================================= */ /** * Store-level permissions. * * Permissions are dynamic: * * resource + action * * Examples: * * sales -> view * sales -> create * sales -> update * sales -> delete * * purchase -> view * purchase -> create * * products -> view * products -> create * products -> update * products -> delete * * inventory -> view * inventory -> adjust * * customers -> view * customers -> create * customers -> update * * reports -> view */ export const storePermissionsTable = pgTable("store_permissions", { id: uuid("id").defaultRandom().primaryKey(), resource: text("resource").notNull(), action: text("action").notNull(), description: text("description"), createdAt: timestamp("created_at", { withTimezone: true, }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true, }).notNull().defaultNow().$onUpdate(() => new Date()), }, (table) => [uniqueIndex("store_permissions_resource_action_unique_idx",).on(table.resource, table.action,), index("store_permissions_resource_idx").on(table.resource), index("store_permissions_action_idx").on(table.action),],);